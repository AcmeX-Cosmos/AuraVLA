# NVIDIA VLM credentials are runtime-only. Copy this file to a local,
# untracked file or export the variables in your shell.
NVIDIA_API_KEY=

# Optional: comma-separated keys for concurrent request failover.
# NVIDIA_API_KEYS=
