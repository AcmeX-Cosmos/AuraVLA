# Robot Configuration
robot:
  name: "aura_robot"
  type: "generic"

  # Workspace limits
  workspace:
    x_min: 0.3
    x_max: 0.8
    y_min: -0.5
    y_max: 0.5
    z_min: 0.0
    z_max: 0.5

  # Joint limits
  joint_limits:
    max_velocity: 1.0
    max_acceleration: 2.0
